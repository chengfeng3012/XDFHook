# XDFHook v1.0.15

## 应用内更新
- 更新说明支持 **Markdown 渲染**（标题 / 粗体 / 列表 / 代码块）
- 更新弹窗提供四个操作：**取消 / 打开 Release 页 / 下载 APK / 下载并安装**
- 支持在应用内下载 APK 并通过 root 静默安装（安装由 system_server 持有事务，宿主进程被杀不影响，也不会出现坏包）

## 操作页
- **Root 开关即面板总闸**：默认关闭且不请求 root；打开时请求授权并显示全部操作按钮，关闭时收起面板
- 修复此前 root 反复探测导致 Magisk 授权提示不断弹出的问题
- 修复操作按钮在无 root 时无法正确置灰的问题
- 静默安装输入框新增「选择」按钮，可直接从文件管理器选取 APK 并自动回填路径

## 选项页
- 日志上限标题标注单位 `(KB)`，并显示当前值
- 日志级别显示当前等级
- 关于与检查更新的版本号改为从 Manifest 动态读取

## 修复
- 分项设置页按返回键不再直接退出应用
- 修复更新弹窗标题占位符未替换、更新日志不显示、"vv1.0.x" 双 v 等问题
- 桌面最近任务加载日志降为 VERBOSE，不再刷屏
