<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="24dp">

        <TextView
            style="@style/PageTitle"
            android:text="@string/tab_overview" />

        <!-- 环境自检 -->
        <LinearLayout style="@style/Card">
            <TextView
                android:id="@+id/env_text"
                style="@style/ItemSubtitle"
                android:layout_width="match_parent"
                android:paddingLeft="16dp"
                android:paddingRight="16dp"
                android:paddingTop="12dp"
                android:paddingBottom="12dp"
                android:text="@string/env_checking" />
        </LinearLayout>

        <TextView style="@style/SectionTitle" android:text="@string/section_module" />

        <!-- 模块：总开关 + 子模块 -->
        <LinearLayout style="@style/Card">
            <LinearLayout style="@style/ItemRow">
                <LinearLayout style="@style/ItemTextCol">
                    <TextView style="@style/ItemTitle" android:text="@string/master_title" />
                    <TextView style="@style/ItemSubtitle" android:text="@string/master_subtitle" />
                </LinearLayout>
                <Switch
                    android:id="@+id/sw_master"
                    style="@style/MiuixSwitch" />
            </LinearLayout>

            <View style="@style/Divider" />

            <LinearLayout
                android:id="@+id/module_list"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </LinearLayout>

        <TextView
            style="@style/Footnote"
            android:text="@string/overview_footnote" />
    </LinearLayout>
</ScrollView>
